<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <!-- Customize your theme here. -->

        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/primary</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/dark_primary</item>


        <!-- colorAccent is used as the default value for colorControlActivated
        which is used to tint widgets -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="colorAccent">@color/dark_primary</item>
        <item name="android:textColorSecondary">@color/secondary_text</item> <!-- Цвет инпутов-->

        <item name="android:colorBackground">@color/active_white</item>
        <item name="android:windowBackground">@color/active_white</item>
        <item name="android:progressBarStyle">@style/ProgressBar</item>
    </style>

    <style name="SwitchCompat" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="colorControlActivated">@color/dark_primary</item>

        <item name="colorControlHighlight">@color/primary</item>

        <item name="colorSwitchThumbNormal">@color/dark_primary</item>
    </style>

    <style name="ProgressBar" parent="android:Widget.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/progress_bar</item>
    </style>

</resources> 